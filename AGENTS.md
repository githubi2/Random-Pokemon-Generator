# AGENTS.md — Random Pokemon Generator 项目规则（Kimi 版）

> **本项目以 SEO 为核心目标。** 所有代码、内容、UI 改动必须围绕 SEO 展开；
> 当任何直觉/便利与 SEO 冲突时，**SEO 优先**。
> 此文件为 **Kimi（本 AI 助手）的项目规则**，由 Hermes 项目规则平移而来，内容完全继承；
> 后续本项目所有开发工作（新页面、改动、上线）均由 Kimi 严格按照本规则执行。

---

## 1. 硬性 SEO 规则（不可违反）

1. **canonical**：一律指向 `https://www.random-pokemon-generator.co/...`（带 www 前缀；子页为 `/nuzlocke-generator/`）。禁止无 www 版本。
2. **Title**：每页唯一、**主词前置**、尽量 ≤60 字符；`og:title` / `twitter:title` 必须与 `<title>` 完全同步。标题可用完整关键词 + 简短副标题（例：`Random Pokemon Generator Wheel - Pokemon Spinner`）。
3. **H1**：每页**恰好 1 个**，必须用**完整目标关键词**（如 `Random Pokemon Generator Wheel`，禁止写 `Pokemon Wheel`）；标题层级 h1→h2→h3 不跳级。所有标题性元素（FAQ H2、How It Works H2）同样用完整关键词。
4. **词数**：每页正文 **≥1200 词**（目标 1200-1800）；新页必须达标后才可提交。
5. **JSON-LD**：每页必须有 `SoftwareApplication` + `FAQPage`（FAQPage 必须与页面可见 FAQ **逐条一致**）；子页另加 `BreadcrumbList`。JSON-LD 的 `name` 必须与完整关键词一致（*不是*短变体）。所有 JSON-LD 必须可解析。
6. **OG/Twitter**：每页必须有 `og:image`（1200×630 绝对 www URL，放页面同目录 `og-image.png`）+ `og:image:width/height` + `twitter:card: summary_large_image` + `twitter:image`；`og:url` 必须带 www。
7. **URL slug 关键词化**：新页面目录名 = **完整目标关键词**（例：`pokemon-smash-or-pass/`、`random-pokemon-generator-wheel/`），**绝不省略关键词中的词**（`smash-or-pass/`、`pokemon-wheel/` ❌）。URL = Title 主词 = H1。
8. **锚文本完整关键词化**：所有引用点（nav、footer、正文语境内链、JSON-LD name、Breadcrumb name）的锚文本必须用**完整关键词**；禁止短变体锚文本（`Pokemon Wheel` ❌ → `Random Pokemon Generator Wheel` ✅）。关键词变体（如中长形式）只允许在**正文叙述**中自然出现，不允许作为锚文本。
9. **内链闭环**：**每个页面必须引用站内所有其他页面**，三层引用缺一不可：nav + footer + **正文语境内链**（锚文本用关键词）。正文互链的判定口径：在剔除 `<script>/<style>` 后的 `<main>` 可见内容里，必须存在指向**其余每一个页面**的至少 1 条语境内链（header logo、nav、footer 里的链接不计入正文层）。新增页面必须同步更新全部既有页面的 nav/footer/正文内链，并在提交前用脚本验证 N×N 正文矩阵无缺口。
10. **sitemap.xml**：新页面必须加入（www URL + `lastmod` 当天，新增页用目录形式 URL）；禁止无 www 条目；robots.txt 的 `Sitemap:` 行保持指向 www sitemap。
11. **链接形式**：站内链接一律**目录形式**，**禁止在 href 中出现 `index.html`**（例：`nuzlocke-generator/`、`../`、`./`）——避免 Google 将 `xxx/index.html` 与 `xxx/` 视为两个 URL 造成重复页面。禁止 `href="/..."` 绝对路径。
12. **敏感字样**：禁止出现 "SEO-optimized"、"free" 等自曝/合规敏感措辞（历史决策：曾全站移除）。标题/描述/正文都不允许。

## 2. 内容规则

- 正文自然覆盖目标长尾词 + 变体，不关键词堆砌；事实数字必须准确（1025 物种、326 形态、18 属性、Gen 1-9）。
- 每个页面回答用户真实意图：工具功能、使用场景、玩法（Nuzlocke、smash or pass、wheel 转盘等）。
- 新增内容段落后同步检查词数与 H2/H3 结构（标题性文字用完整关键词）。
- 正文内链放语境内（自然句子里嵌关键词锚文本），不单独堆链接行。

## 3. 工程约定

- 纯静态 Vanilla JS（ES5 IIFE 风格，无构建步骤、无依赖）。
- **禁止原生 `alert` / `confirm` / `prompt`（除非剪贴板兜底）**——一律使用全站自定义 modal 风格（`.modal-overlay` + `.modal` + 品牌按钮）。新页面交互同理。
- **ES5 陷阱**：禁止函数名与局部变量同名（`var pool = pool();` 类 var 提升遮蔽会导致 TypeError——已踩过）。
- **hero 区规范**：hero 只保留 eyebrow + H1 + lede，**不放 CTA 按钮**（工具入口在工具面板内，避免重复入口）；与既有页面视觉一致。
- 移动端（≤760px）必须适配（汉堡导航、历史/列表紧凑布局；nav 6 项完整关键词在桌面可用、移动端折叠）。
- **`vercel.json`**：新增页面目录后，必须在 `redirects` 加 `/{dir}/index.html → /{dir}/` 的 permanent 重定向。
- **缓存策略（vercel.json `headers`，2026-09-05 起）**：CSS/JS 浏览器缓存 1 天 + SWR 7 天；图片/音频（png/ico/svg/wav）缓存 7 天 + SWR 30 天；HTML 保持 Vercel 默认 `must-revalidate`。改动 CSS/JS 后**必须 bump 引用处的 `?v=` 版本号**（既有约定），新页面的脚本/样式引用一律带版本号。
- 修改后必须验证：`node --check` JS、HTML 标签配对、canonical/词数/内链矩阵/JSON-LD 回归（用 `kimi-verify-` 前缀临时脚本，跑完即删）。
- 文件名/目录：子页面用目录形式（`pokemon-smash-or-pass/index.html` → URL `/pokemon-smash-or-pass/`）。

## 4. 上线流程（新页面清单）

1. 页面文件 + JS + `og-image.png`（1200×630 同目录）
2. `sitemap.xml` 加新 URL（www + lastmod 当天）
3. `vercel.json` 加 `/{dir}/index.html → /{dir}/` 永久重定向
4. **全部既有页面**更新 nav + footer + 正文内链（新页关键词锚文本）
5. JSON-LD ×3（SoftwareApplication / FAQPage 一致 / BreadcrumbList）
6. `<title>`/`og:title`/`twitter:title` 三同步 + description **100-160 字符**（160 是 SERP 截断硬线，不是 170；mega 页与 name 页曾因 160+ 被点名。写法：完整关键词开头命中 + 只保留最核心差异化卖点，尾部功能罗列宁删勿超）
7. 正文 **≥1200 词**（目标 1200-1800）+ 内外链闭环；**正文软上限 ~1800 词**（超过不致命但会稀释主题，写正文时控制在 1200-1700 区间；宁可句句扎实，不为凑数注水）
8. 移动端适配（汉堡导航、按钮触屏 ≥44px）
9. 禁原生弹窗（modal 风格）
10. **更新 README.md**：新页面必须同步进 README——"What's inside" 表格加一行（emoji + 页面名 + 主站绝对 URL dofollow 链接 + 一句话描述）+ 项目结构树补新目录；高优先级内容页另加独立小节介绍。README 是 GitHub 高权重域名指向全站子页的外链来源，漏更等于白丢外链。
11. 推送后验证：308 重定向 / **og-image 直接 curl 确认 200**（源码声明≠文件存在，分享封面 404 会白丢 Discord/Reddit/Twitter 的自然传播流量）/ 页面 200 / GSC 提交

- 提交信息用英文、语义化；推送后 Vercel 自动部署。
- 上线后验证线上：curl 检查 title/canonical/sitemap/重定向一致性。
- GSC 操作（人工）：sitemap 变更后重新提交 `https://www.random-pokemon-generator.co/sitemap.xml`。
- **FAQ 是金矿**：FAQ 里回答得扎实的"工具向搜索问题"（如 shiny 页的 "What are the odds of a real shiny Pokemon?" 覆盖 1/8192 → 1/4096 → Shiny Charm 1/1365 → Masuda 1/683 → 叠加 1/512）本身就是有搜索量的词。复盘时逐条审视 FAQ，凡能独立的都记入下方候选词清单。
- **内链姿势（已验证有效，继续保持）**：How It Works 收尾段 + footer 用精确锚文本把全站工具串成闭环。

## 5. 候选关键词池（复盘沉淀，未来可做独立页）

> 全站已上线页面的关键词基线见 `SEO-KEYWORDS.md`；新词上线后从本池移出并补进基线表。

- ~~`pokemon shiny odds` / `shiny odds`~~ —— ✅ 已上线：`/pokemon-shiny-odds/`（2026-09-03，内容页+交互计算器）。

### 2026-09-05 调研补充（SERP 实查，非工具估计）

- ~~`pokemon nature chart` / `pokemon natures`~~ —— ✅ 已上线：`/pokemon-nature-chart/`（2026-09-05，内容页+交互速查表+按宝可梦推荐性格）。上线依据（哥飞 KD 数据推翻原判断）：KD 25.5 容易、月搜 40.5K、前十有 crob.at DR 1 排 #2 / pokemoneros.com DR 2 排 #5，弱位可打。SERP 意图实查 = 性格（25 natures ±10%），**非属性克制**——属性克制内容刻意留给 type chart 页。
- ~~`birthday pokemon`~~ —— ❌ 放弃（KD 数据实锤，2026-09-05）：EMD 品牌站 birthdaypokemon.com（DR 39）卡 #1 享品牌位保护；可竞争位最弱对手 dragonflycave.com DR 52，DR=0 新站需 50~110 引用域预算；搜索量 27.1K→12.1K 走低（不排除 Pokémon Day 季节性，但前两条已足够否决）。原"WikiHow 缺口"判断作废。
- `pokemon type chart` —— 🥈 长线目标（非放弃）。月搜 201K、KD 37.2，但 #1 pokemondb DR 71 + #3 pkmn.help 强产品力镇守，201K 量级词 EEAT 门槛高。打法：先靠 nature chart 页攒流量外链把全站 DR 拉到 15~20，再做 `/pokemon-type-chart/`（18 属性克制矩阵 + 双属性计算器，内容与 nature 页零重叠）。
- `pokemon would you rather` —— ❌ 放弃（2026-09-05 实查：月搜仅 260、KD 52.3，投入产出灾难级；原"内容成本高"判断不变）。
- `pokemon fusion generator` —— ❌ 放弃。Semrush 实测美区 8.1K/月，但融合立绘无合规素材来源，纯前端无法实现。（KD 33、前十有 DR 2 弱站，数据上本可做，版权红线绕不过。）
- `full odds shiny` —— ❌ 放弃（KD 过高，pokemon.com / pokemondb / marriland 垄断，同 shiny odds 页备注结论）。
- `pokemon randomizer` —— ❌ 放弃。量虽大但意图是 ROM 修改器下载，与网页工具错配，高跳出。

---

## 6. SEO 标准库（哥飞知识库 83 条 · 逐条必守）

> 来源：`SEO-STANDARD-83.xlsx`（AI网站SEO标准·83条·哥飞知识库逐条验证，2026-09-06 并入本规则）。
> **执行口径**：后续所有开发/改版/内容/上线，除第 1-5 节外，必须逐条对照本节；
> 与本文件第 1 节重复之处以第 1 节精确口径为准（如 description 100-160 字符）。
> 标注：【已有】=第 1-5 节已强制；【新增】=本版新增必守；【✅】=现状已符合；【🔧】=现状有差异，需整改或验收确认。

### 6.1 选词（R1）
- R1.1 先找词再建站 —— 从真实被搜索且竞争不大的关键词出发做页面。【已有：第 5 节关键词池即选词结论】
- R1.2 KGR 判蓝海 —— KGR = intitle结果数 ÷ 月搜索量；<0.25 或 intitle<100 为蓝海。【新增：新词立项必算】
- R1.3 KD 判竞争 —— 看前 20 名网页竞争力（外链/DR/页权/首页 vs 内页），无统一公式。【已有：第 5 节「SERP 实查」法即此】
- R1.4 KGR 与 KD 是两回事 —— KGR=供应量，KD=前排竞争力，无正相关，必须同时看。【新增】
- R1.5 KDRoi 立项公式 —— KDRoi = 搜索量 × CPC ÷ KD，综合性价比最高才做。【新增】
- R1.6 KD 分级阈值 —— KD<20 极易 / 20-39 容易 / 40-59 中等 / 60-79 困难 / ≥80 极难。新站只做 KD<40。【新增：本项目 DR≈0，锁定 KD<40】
- R1.7 新词优先 —— 过去 12 个月才出现的词=竞争空白，新站可上线即排名。【新增：与 R15.2 呼应】

### 6.2 TDH 页面结构（R2）
- R2.1 不写 Keywords 标签 —— 10 年前被谷歌淘汰，SEOer 蹭词致不信任。【✅ 已整改 2026-09-06：全站 18 页 grep=0，无 keywords 标签】
- R2.2 Title 是最核心 SEO 元素 —— 给搜索引擎看也吸引点击。【已有：规则 2】
- R2.3 Title 写法 —— 拼上用户关心+有搜索量的多个点，不是大站式只写名字。【已有：规则 2（完整关键词+副标题）】
- R2.4 Description 认真写 —— 出现在搜索结果里，放 Title 放不下的补充信息。【已有：规则 4 节第 6 条，100-160 字符】
- R2.5 H1 只能有 1 个 —— Title 给搜索引擎看，H1 给用户看，两者决定排不排名。【已有：规则 3】
- R2.6 H2/H3 是骨架 —— H1-H6 是网页骨骼结构，语义化层级支撑内容。【已有：规则 3 不跳级】
- R2.7 正文进 p、图片进 p —— 标题归 H，正文归 p，图片可放 p 内。【新增】
- R2.8 img 必写 alt + 用大图 —— alt 给搜索引擎看；≥300×300 吃图片搜索流量。【✅ 已整改 2026-09-06：全站 img 均带 alt；og 1200×630、内容图 1200×980/1446×710 ≥300×300】
- R2.9 典型页面骨架 —— h1→h2→p→h3→p（h3 又 h3→h2 分块）。工具/内容页全要素。【已有：各页 How It Works/FAQ 结构即此】

### 6.3 技术规范（R3）
- R3.1 新站必须 SSR/后端渲染 —— TDK/H1-H6/正文必须 HTML 源码可见。引擎不会给新站花算力跑 JS。【✅ 本项目纯静态 HTML=源码可见，天然满足；禁止改成客户端-only 渲染】
- R3.2 canonical 确保 URL 唯一 —— head 加 canonical；www/裸域/http/https/尾斜杠统一。【已有：规则 1】
- R3.3 分页 canonical 特殊规则 —— 第 2 页起 canonical → 第 1 页 URL，Title 写页码。【新增：本站无分页；未来做列表分页必守】
- R3.4 上线必做 sitemap + 提交 GSC —— sitemap.xml → GSC 提交。【已有：规则 10 + 上线清单第 11 条】
- R3.5 全站放流量统计 —— GA 代码放 </body> 前。【✅ 已验收 2026-09-06：GA=head+async（Google 官方推荐，async 不阻塞渲染）；CWV 待 PSI 人工复核】
- R3.6 sitemap 无法抓取 5 种原因 —— ①拿网页地址提交②未放根目录③格式错误④域名不一致⑤后缀 Bug(.cc)。【已有：sitemap 在根目录 + www URL；改 sitemap 后自查这 5 项】

### 6.4 内链结构（R4）
- R4.1 主词/二级/三级分层 —— 首页主词→子目录二级词→再下层三级词→内页承接。【已有：规则 9 闭环的语义化版】
- R4.2 首页六字真言 —— 分门别类罗列：h1 主词 + h2 二级 + h3 三级 + 图片。【✅ 已验收 2026-09-06：h1=完整主词 + h2 分类（How It Works/FAQ/Roll History）+ h3 分块 + 卡片图】
- R4.3 内链双向贯通 —— 三级页→二级页→首页；全链回首页；锚文本用上级词。【已有：规则 8/9】
- R4.4 内链重要性不亚于外链 —— 内链是站内 SEO 核心之一，地位与外链相当。【理念；规则 9 保证】
- R4.5 任何链接都传递权重 —— 权重方向=链接指向方向。首页最高因反链+内链最多。【已有：规则 9 闭环】
- R4.6 面包屑导航必须 —— 每内页有面包屑 + JSON-LD BreadcrumbList 结构化数据。【已有：规则 5 子页 BreadcrumbList；✅ 已验收 2026-09-06：7 个工具子页均有面包屑 UI + BreadcrumbList JSON-LD】
- R4.7 全站顶部导航条 —— 树根关键词做全站顶部导航，每页都给频道页加内链。【已有：nav.js 全站顶部导航】

### 6.5 外链标准（R5）
- R5.1 外链=权重背书 —— 高质量外链是推荐信/选票，提升权威度和排名。【理念】
- R5.2 抄同行外链 SOP —— 分析竞品外链→抄路径（博客/论坛/导航站分类复用）。【新增：纳入周常外链任务】
- R5.3 启动外链加速收录 —— 新站去谷歌常爬站发帖+老站加友链。【新增】
- R5.4 某些导航站外链别花钱 —— 哥飞专题：什么样导航站外链千万不要花钱买。【新增：采购外链前先看这条】
- R5.5 nofollow 已非硬指令 —— 谷歌把 nofollow 当建议而非命令。【新增】
- R5.6 锚文本自然可控 —— 尽量带锚文本，很多站只支持部分格式需逐个试。【新增：外链锚文本尽量带关键词】

### 6.6 内容质量（R6）
- R6.1 页面字符量 ≥800 —— 工具页+内容页都适用。【已有：规则 4 词数 ≥800】
- R6.2 核心词密度 3%~5% —— 围绕核心关键词的密度控制。【新增：正文主词出现次数 ≈ 词数×(3%~5%)；与规则 8 锚文本不冲突】
- R6.3 结构化数据 JSON-LD —— 放 head。品牌 Organization 必标，面包屑 BreadcrumbList 必标。FAQPage 富媒体 2026-05-07 下线但内容模块保留。【已有：规则 5；✅ 已整改 2026-09-06：18 页全部含 Organization JSON-LD】
- R6.4 OpenGraph+TwitterCard —— 全页统加社交分享元标签。【已有：规则 6】
- R6.5 内容真实有效 —— 功能服务和内容真实有效→谷歌收录给排名。不造假。【理念；与 R6.9 联动】
- R6.6 一个页面一个关键词 —— 每个内页聚焦一个核心词→围绕它展开解释说明。【已有：规则 3/7/8】
- R6.7 先有词再有页面 —— 用关键词库找到用户搜什么→再为词做精准承接页。质量>数量。【已有：第 5 节流程】
- R6.8 一文一义 —— 每页有明确单一主旨语义。不相干词蹭排名=谷歌语义分析拒绝。【新增：验收时检查每页单一主旨】
- R6.9 博客内容策略 —— 600-1000 字；真实场景问答；≥8 竞品链接增可信度；产品丝滑融入。【✅ 已整改 2026-09-06：3 篇补域外权威/竞品链接 8/8/9 个（≥8）；字数 1316-1418（900-1700 区间）；站内产品融入原有；链接均为验证有效 URL】
- R6.10 低质量页面污染整站 —— 已抓取未索引=低质量判定。持续生成→整站标记→清除已有索引。【新增：宁可少做不做烂页；未达标页不上线】

### 6.7 收录索引（R7）
- R7.1 收录延迟多因质量问题 —— 已发现/已抓取未索引=内容质量不达标，先优化内容不堆外链。【新增】
- R7.2 手动加速收录 —— GSC test live url → request indexing。内容不行手动也没用。【新增：新页上线后人工 GSC 提交，见上线清单】
- R7.3 不收录先归因 —— 查 HTML 有无正常内容→核心页是否空模板→空模板被拒收。【新增：排查顺序】
- R7.4 新站有波动 —— 核心页被摘索引可能是新站正常波动，先看整体权重不急着弃域名。【新增：心态规则】

### 6.8 GSC 监控（R8）
- R8.1 哥飞网站权重公式 —— 权重分 =（有曝光点击页数 ÷ 爬虫发现总页数）× 10000。及格 ≥100。【新增：GSC 周度指标】
- R8.2 每日关注未索引量 —— 持续上升=生成策略有问题，立刻停手。【新增】
- R8.3 先试探再放量 —— 网页 <1 万时试探谷歌→确认策略有效→再放大。【新增：本站页面 <100，每批 ≤5 页试探】

### 6.9 多语言国际化（R9）
- R9.1 Hreflang 设置 —— 每语言页互设 hreflang + self-referencing canonical。英文放 / 根目录。【新增：当前英文单语站暂不适用；若加语言必守】
- R9.2 语言代码规范 —— 不分地区=语言码(en/zh)；分地区=完整码(zh-CN/zh-Hant-TW)。【新增：同上】
- R9.3 先做英文再扩多语言 —— 锁定英语市场先拿验证数据→不要一上来就多语言。【✅ 现站即英文单语，符合】

### 6.10 重定向 404（R10）
- R10.1 页面下线主动 404 —— 完全正确→谷歌如实反馈状态码→自行判断是否修复。【新增：下线页真 404 而非软 200】
- R10.2 URL 改版优先不改 URL —— 只改内容不动 URL。必须改 URL 时用 301。【已有：规则 8 节关键词重定位时 slug 保持（skill 经验同源）】
- R10.3 www/裸域统一 —— 选一种作主域名→另一种 301 重定向。【已有：www 为主（规则 1）；✅ 已整改 2026-09-06：实测裸域 https/http 均 308→www，www 200】
- R10.4 过期域名 301 策略 —— 买高度相关过期域名 301→权重瞬间转移。但必须 WaybackMachine 查历史防博彩色情站。【新增：未来收域名必查】

### 6.11 robots.txt（R11）
- R11.1 质量不可控页面屏蔽 —— UGC 源页面/不可控目录→整个目录 robots 禁止抓取。【新增：本站无 UGC；临时目录遵循】
- R11.2 规划排名页 vs 非排名页 —— 建站时规划：只把获取排名用页面给谷歌，其余不给。【新增：非排名页用 robots/404 隔离】
- R11.3 误屏蔽排查 —— robots 用错会误屏蔽应被索引页面→GSC 提示立刻修复。【新增：改 robots 后 GSC 复查】

### 6.12 移动端性能（R12）
- R12.1 移动优先索引 —— 谷歌用移动优先→必须响应式设计+多设备正常。【已有：≤760px 适配规则】
- R12.2 Core Web Vitals 达标 —— LCP/CLS/INP 用 PageSpeed Insights 检测修复。【新增：大改后跑 PSI；目标 LCP<2.5s、CLS<0.1、INP<200ms】
- R12.3 延迟加载有坑 —— Next.js 首页动态 import 做懒加载→反而性能大幅下降。【⚠️ 警示：本站禁止动态引入核心脚本，保持同步脚本顺序】
- R12.4 图片格式优化 —— 优先 WebP/AVIF；文件名用关键词；≥300×300。【✅ 已确认 2026-09-06：存量图均小 PNG（24-58KB）无需转 WebP；尺寸/文件名达标；新图按 WebP+关键词名执行】
- R12.5 资源优化 —— CDN/压缩/代码分割/预加载关键资源/响应式全屏幕。【新增：Vercel CDN 已带；控制单页资源数量】

### 6.13 安全基础设施（R13）
- R13.1 必须全站 HTTPS —— Cloudflare 免费 SSL→全站强制 HTTPS→谷歌排名信号。【✅ Vercel 默认 HTTPS】
- R13.2 建议用 Cloudflare —— 免费 CDN 275+ 节点/DDoS 防护/SSL/WAF/Worker 10 万次/天。【新增：可选接入；域名 DNS 在 Cloudflare 时启用】
- R13.3 域名注册选 Namesilo —— 价格透明续费不涨价/免费 WHOIS 隐私/双因素认证/至少买 2 年。【新增：域名续费策略】

### 6.14 Adsense 变现（R14）
- R14.1 先流量后广告 —— 有流量再放广告。离钱近 ecpm 高：英文站最高 $10 vs 中文站 $0.54。【✅ 英文站；有流量后再接】
- R14.2 审核两步走 —— 先审账号→再网站。满 $10 寄 PIN 平信→3 次后可人工审核。【新增：若接入按此走】
- R14.3 内容影响过审 —— 核心页缺关键词/无 FAQ/Blog/HowTo/只靠 AI 生成→审核无望。【✅ 本站核心词+FAQ+How It Works 齐备，天然有利】

### 6.15 冷启动节奏（R15）
- R15.1 三步走节奏 —— 找需求做站→搞流量→变现。第一版无需登录注册，Web1.0 静态页上线。【✅ 与本站形态完全一致】
- R15.2 新站成功公式 —— 新站秘诀=新词+快速上线=工具+内容+多语言。15% 搜索是新词。【✅ 本站即此路线；持续挖新词】
- R15.3 做好页面再放量 —— 用关键词库找用户搜什么→每页都奔着关键词排名和流量。【已有：全文贯彻】

### 6.16 GSC 实操（R16）
- R16.1 GSC 数据有延迟 —— 不是实时的，当天下降过几天可能修正→不慌张改策略。【新增：数据解读规则】
- R16.2 低排名先提排位 —— 前十但 CTR 低→核心目标是提排名，不是纠结 CTR。【新增：优化优先级】
- R16.3 不依赖 Disavow —— 谷歌已不依赖 Disavow 处理恶意外链→不用花时间提交。【新增：省时间】

### 6.17 内容型工具站（R17）
- R17.1 不为每条 UGC 生成页面 —— 每 UGC 单独一页→重复/不聚焦/密度不够→改聚合页。【新增：本站无 UGC；若加用户生成内容遵循】
- R17.2 关键词内容关联阈值 —— 关联 UGC ≥3 条（最好 5-10）再生成页→围绕核心词解释+列出内容。【新增：同上】
- R17.3 关键词挖掘循环法 —— 竞对→Semrush 出词→分析页面→更多站→循环→大词库。【新增：常态化选词工作流】
- R17.4 关键词打标方法 —— 关键词搜索+向量相似度+GPT 打标三管齐下→分门别类罗列。【新增：词库维护方法】

---

**执行红线（本节落地口径）**：
1. 新页面/改版/博客落笔前：先过 6.1 选词 + 6.6 内容质量；上线前全量核对 6.2/6.3/6.4/6.12。
2. 任何页面上线即满足：R2.1 无 keywords 标签 / R2.8 图均带 alt / R6.3 Organization+BreadcrumbList / R12.2 CWV 达标。
3. 【🔧】条目为现状待整改项：逐项落账整改，完成一项在本节勾掉一项（不改编号，括号内标注 ✅）。
4. R17.3 关键词挖掘循环法 = 第 5 节关键词池的日常运行机制：竞对 → Semrush 出词 → 分析页面 → 循环。
